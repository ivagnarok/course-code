name: eventos
on: [push, pull_request, issues]

jobs: 
    checkouts-tesst:
        runs-on: ubuntu-latest
        steps:
            - run: |
                git $GITHUB_SHA
                git $GITHUB_REF
            - uses: actions/checkout@v3